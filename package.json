{
  "name": "@ianwremmel/proj",
  "version": "0.0.0-development",
  "description": "",
  "bin": {
    "proj": "./bin/proj"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ianwremmel/proj.git"
  },
  "scripts": {
    "commitmsg": "npm run --silent lint:commitmsg",
    "coveralls": "nyc report --reporter=text-lcov | coveralls",
    "lint": "npm run --silent lint:js && npm run --silent lint:changelog && npm run --silent lint:deps",
    "lint:changelog": "commitlint --from origin/master --to HEAD",
    "lint:commitmsg": "commitlint -e",
    "lint:eslint": "eslint --ignore --ignore-path .gitignore --fix",
    "lint:js": "npm run --silent lint:eslint -- .",
    "lint:deps": "npm run --silent lint:deps:missing && npm run --silent lint:deps:unused",
    "lint:deps:missing": "echo 'skipping dep check because acorn does not handle spread in Objects' # dependency-check package.json src/commands/*",
    "lint:deps:unused": "echo 'skipping dep check because acorn does not handle spread in Objects' # dependency-check package.json --unused --no-dev --ignore-module request --ignore-module yargs src/commands/*",
    "lint:staged": "lint-staged",
    "mocha-reporter-options": "[ -n \"${CI}\" ] && echo '--reporter xunit --reporter-options output=reports/junit/test.xml'",
    "precommit": "npm run --silent lint:staged",
    "test": "nyc --reporter=text mocha $(npm run --silent mocha-reporter-options) './src/**/*-spec.js'",
    "semantic-release": "semantic-release"
  },
  "engines": {
    "node": ">=8.5.0"
  },
  "keywords": [],
  "author": "Ian W. Remmel <design@ianwremmel.com>",
  "license": "MIT",
  "dependencies": {
    "@ianwremmel/pkgshift": "^1.1.1",
    "@octokit/rest": "^15.2.2",
    "chalk": "^2.3.0",
    "debug": "^3.1.0",
    "invariant": "^2.2.2",
    "lodash": "^4.17.4",
    "mkdirp": "^0.5.1",
    "mz": "^2.7.0",
    "netrc": "^0.1.4",
    "request": "^2.83.0",
    "request-promise-native": "^1.0.5",
    "supports-color": "^5.0.1",
    "yargs": "^11.0.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^6.0.2",
    "@commitlint/config-angular": "^6.0.2",
    "@ianwremmel/eslint-config-base": "^1.0.0",
    "@ianwremmel/eslint-config-standard": "^1.2.0",
    "babel-eslint": "^9.0.0",
    "chai": "^4.1.2",
    "condition-circle": "^2.0.0",
    "coveralls": "^3.0.0",
    "dependency-check": "^3.0.0",
    "eslint": "^4.11.0",
    "eslint-plugin-import": "^2.8.0",
    "eslint-plugin-mocha": "^5.0.0",
    "husky": "^0.14.3",
    "lint-staged": "^7.0.0",
    "mocha": "^5.0.0",
    "nyc": "^11.2.1",
    "semantic-release": "^15.0.2"
  },
  "lint-staged": {
    "*.js": "lint:eslint",
    "bin/**": "lint:eslint"
  },
  "release": {
    "verifyConditions": "condition-circle"
  }
}
